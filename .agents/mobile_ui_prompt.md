# 🎨 [디자인 & UI/UX 특화] 모바일 UI 전 화면(20개) 프롬프트 가이드

본 문서는 로직이나 백엔드 연동 지시 대신 **모바일 화면의 비주얼 디자인, 레이아웃 구획, 컬러 팔레트, 타이포그래피, 애니메이션 및 그래픽 UI/UX 퀄리티를 극대화**하기 위한 전용 프롬프트 가이드입니다.

---

## 💎 0. 모바일 디자인 시스템 명세 (Visual Design System)

AI 디자인 및 코드 생성 도구(Antigravity, Figma AI, Cursor)에 기본 전달되는 **프리미엄 모바일 UI 디자인 토큰**입니다:

- **Primary Color**: `#2A60F7` (KB Blue 트렌디 블루), Active: `#1D4ED8`, Subtle: `#EFF6FF`
- **Safety Status Color**: `#10B981` (안전 🟢), `#F59E0B` (주의 🟡), `#EF4444` (위험 🔴)
- **Background & Card**: App Bg `#F8FAFC`, Card Surface `#FFFFFF`, Glass Overlay `rgba(255, 255, 255, 0.85)` (Blur 12px)
- **Border & Shadow**: Soft Border `#E2E8F0`, Card Elevation `0 4px 20px -2px rgba(15, 23, 42, 0.06)`, Floating Sheet Shadow `0 -6px 30px rgba(0,0,0,0.1)`
- **Radius**: Large Cards `20px`, Inputs/Buttons `14px`, Small Badges `8px`
- **Typography Hierarchy**: Display Title 24px Bold, Section Title 18px SemiBold, Body 14px Regular, Caption 12px Medium

---

## 📱 PART 1. 회원가입 및 본인인증 (3개 화면)

### 🎨 화면 01: 로그인 UI 디자인

```markdown
[디자인 목표] 신뢰감을 주는 깔끔하고 간결한 모바일 로그인 UI를 디자인해 줘.

[시각적 레이아웃 및 디자인 스펙]

1. Header Visual: 브랜드 로고 "살고싶오 🛡️" + "낮선 동네도 안심하고 이사하세요" (Bold 22px, #0F172A)
2. Input Form Fields:
   - 로그인 아이디 입력창 (Placeholder: "아이디를 입력하세요")
   - 비밀번호 입력창 (Placeholder: "비밀번호를 입력하세요")
   - Focus 시 Border #2A60F7 + Subtle Blue Box-Shadow (0 0 0 3px #EFF6FF) 효과
3. Action Links & CTA:
   - [아이디 찾기 / 비밀번호 찾기] 하단 서브 텍스트 링크
   - 56px 높이 Full-Width 대형 Primary Blue 버튼 ("로그인")
   - "계정이 없으신가요? 회원가입" 하단 가입 전환 링크
```

### 🎨 화면 02: 회원가입 폼 UI 디자인 (이름, ID, 이메일, 생년월일, 성별, 비밀번호)

```markdown
[디자인 목표] 깔끔하고 접근성 높은 회원가입 폼 UI 디자인. (주민등록번호/휴대폰 인증 없음)

[시각적 디자인 스펙]

1. Header: 뒤로가기 버튼 + "회원가입"
2. Input Form Fields:
   - 이름 입력창 (Placeholder: "홍길동")
   - 로그인 아이디 입력창 + 우측 44px 높이 `[중복확인]` Outlined 버튼
   - 이메일 입력창 (Placeholder: "example@email.com")
   - 생년월일 입력창 (YYYY-MM-DD) & 성별 선택 Segmented Control (`[남] / [여]`)
   - 비밀번호 (8자 이상) & 비밀번호 확인 입력창
3. Bottom Fixed CTA: 모든 필수 항목 입력 시 활성화되는 56px 대형 Primary Blue 버튼 ("회원가입 완료")
```

### 🎨 화면 03: 아이디 / 비밀번호 찾기 모달 UI 디자인

```markdown
[디자인 목표] 중앙 레이어드 모달 스타일 계정 찾기 UI 디자인.

[시각적 디자인 스펙]

1. Modal Backdrop: Dimmed Overlay (`rgba(15, 23, 42, 0.5)` Blur 8px)
2. Tab Control: `[아이디 찾기]` / `[비밀번호 찾기]` 상단 탭
3. Input & Result:
   - 아이디 찾기: 이름 + 이메일 입력 시 마스킹 처리된 아이디(예: `ahs0***`) 결과 출력
   - 비밀번호 찾기: 비밀번호 재설정 폼 노출
```

---

## 🧭 PART 2. 온보딩 주거 조건 설정 (5개 화면)

### 🎨 화면 04: 온보딩 Step 1 - 목적지 설정 UI 디자인

```markdown
[디자인 목표] 공통 온보딩 프레임 안에서 목적지를 입력·선택하는 주소 검색 카드 UI 디자인.

[시각적 디자인 스펙]

1. Fixed Header: 좌측 뒤로가기, 중앙 타이틀 `나에게 맞는 매물 찾기`, 우측 현재 단계 표시와 Blue Progress Bar를 상단 고정
2. Content Card: 화면 중앙의 흰색 라운드 카드 안에 아이콘, 질문 문구, 보조 설명 배치
3. Purpose Selector: 학교·직장·기타를 아이콘형 선택 버튼으로 구성하고, 활성 항목은 Blue Border·Subtle Blue Background 적용
4. Address Search: 돋보기 아이콘·Clear(X)가 있는 목적지 검색 Input과, 입력 중 자동완성되는 주소 선택 리스트 배치
5. Fixed Bottom CTA: `[다음]` Full-Width Primary 버튼을 화면 하단에 고정
```

### 🎨 화면 05: 온보딩 Step 2 - 통근 수단 및 소요시간 UI 디자인

```markdown
[디자인 목표] 공통 온보딩 프레임 안에서 이동 수단과 소요 시간을 설정하는 카드 UI 디자인.

[시각적 디자인 스펙]

1. Fixed Header: 뒤로가기, `나에게 맞는 매물 찾기` 타이틀, 단계 표시와 Progress Bar를 상단 고정
2. Content Card: 중앙 질문 카드에 이동 수단 선택 영역을 배치
3. Transport Toggle: 도보·대중교통을 아이콘이 포함된 2개 선택 버튼으로 구성하고 활성 상태는 Blue 강조
4. Fixed Bottom CTA: `[이전]` 보조 버튼과 `[다음]` Primary 버튼을 화면 하단에 함께 고정
```

### 🎨 화면 06: 온보딩 Step 3 - 예산 범위 듀얼 슬라이더 UI 디자인

```markdown
[디자인 목표] 공통 온보딩 프레임 안에서 보증금과 월세 예산을 설정하는 카드 UI 디자인.

[시각적 디자인 스펙]

1. Fixed Header: 뒤로가기, `나에게 맞는 매물 찾기` 타이틀, 단계 표시와 Progress Bar를 상단 고정
2. Content Card: 중앙 질문 카드 안에 보증금과 월세 항목을 분리하고 현재 선택 범위를 강조 텍스트로 표시
3. Budget Range: 각 예산 항목에 최소·최대값 라벨과 듀얼 노브 Range Slider를 배치
4. Selection Feedback: 조절 중인 값은 Blue 강조와 Floating Tooltip 또는 금액 뱃지로 즉시 피드백
5. Loan Recommendation Panel: 보증금·월세 예산 범위 아래에 KB 대출 상품 추천 패널 배치. 예상 대출 한도, 금리, 월 상환 예상액 등 핵심 정보를 요약하고 `[상품 자세히 보기]` 링크 제공
6. Fixed Bottom CTA: `[이전]` 보조 버튼과 `[다음]` Primary 버튼을 화면 하단에 함께 고정
```

### 🎨 화면 07: 온보딩 Step 4 - 안전 기준 선택 UI 디자인

```markdown
[디자인 목표] 공통 온보딩 프레임 안에서 원하는 안전 기준을 선택하는 버튼형 카드 UI 디자인.

[시각적 디자인 스펙]

1. Fixed Header: 뒤로가기, `나에게 맞는 매물 찾기` 타이틀, 단계 표시와 Progress Bar를 상단 고정
2. Content Card: 중앙 질문 카드에 안전 선호 수준을 설명하는 제목과 보조 문구 배치
3. Safety Choice Buttons: `매우 안전한 곳`, `적당히 안전하면 돼요`처럼 안전 기준을 설명·상태색이 포함된 큰 선택 버튼으로 구성
4. Selected State: 선택된 버튼은 Blue Border·Blue Tint로 강조하고, 선택 기준에 따른 짧은 안내 문구 표시
5. Fixed Bottom CTA: `[이전]` 보조 버튼과 `[다음]` Primary 버튼을 화면 하단에 함께 고정
```

### 🎨 화면 08: 온보딩 Step 5 - 설정 조건 요약 카드 UI 디자인

```markdown
[디자인 목표] 공통 온보딩 프레임 안에서 모든 설정을 확인하고 매물 탐색으로 이어지는 완료 카드 UI 디자인.

[시각적 디자인 스펙]

1. Fixed Header: 뒤로가기, `나에게 맞는 매물 찾기` 타이틀, 마지막 단계 표시와 완료된 Progress Bar를 상단 고정
2. Content Card: 완료 체크 아이콘, `설정 완료!` 제목, 매물 추천 안내 문구를 중앙 카드 상단에 배치
3. 조건 요약: 목적지, 이동 수단·시간, 보증금, 월세, 안전 기준을 아이콘+값의 행 리스트로 구성
4. Edit Link: 각 조건 행을 수정 가능한 보조 링크 또는 Chevron 형태로 표현
5. Fixed Bottom CTA: `[이전]` 보조 버튼과 `[지도에서 매물 보기]` Primary 버튼을 화면 하단에 함께 고정
```

---

## 🗺️ PART 3. 지도 기반 탐색 및 필터 바텀시트 (5개 화면)

### 🎨 화면 09: 메인 이소크론 지도 탐색 UI 디자인

```markdown
[디자인 목표] 세련된 지도 오버레이와 디자인 핀(Pin) UI.

[시각적 디자인 스펙]

1. Map Canvas: 실제 지도 위에 이동 가능 영역을 반투명 Blue 이소크론 폴리곤으로 표시
2. Top Controls: 좌측 `[필터]` 버튼과 이소크론 영역 표시를 켜고 끄는 원형 체크 버튼만 지도 위 Floating UI로 배치
3. 매물 Pin: 안전 점수·가격 정보를 담은 말풍선형 핀을 상태색으로 구분
4. Listing Sheet: 화면 하단에 드래그 핸들과 매물 미리보기를 둔 바텀시트 배치. 아래에서 위로 드래그하면 매물 목록을 확장해 확인
```

### 🎨 화면 10: 지도 필터 바텀시트 Step 1 UI 디자인

```markdown
[디자인 목표] 설정된 조건을 확인하고 전체 필터를 조절하는 스크롤형 필터 바텀시트 UI 디자인.

[시각적 디자인 스펙]

1. Bottom Sheet: 필터 버튼 터치 시 지도 위로 올라오는 흰색 라운드 시트. 상단에 Drag Handle, 내가 설정한 목적지·이동 시간·예산 정보, 닫기(X) 버튼 배치
2. Filter Tabs: `[전체 필터]`와 `[편의시설 필터]`를 탭으로 구성하고, 활성 탭은 Blue 텍스트·Underline으로 강조
3. 전체 필터 Content: 보증금·월세 범위를 현재 값 라벨과 듀얼 노브 슬라이더로 조절
4. Scroll Flow: 바텀시트 내부를 아래로 스크롤하면 안전 점수·이동 시간 등 추가 전체 필터가 이어져 화면 11의 내용으로 노출
5. Bottom Action: `[초기화]`와 `[적용완료]` 버튼을 시트 하단에 고정
```

### 🎨 화면 11: 지도 필터 바텀시트 Step 2 (안전 상세 필터) UI 디자인

```markdown
[디자인 목표] 전체 필터의 스크롤 연속 영역과 편의시설 탭 콘텐츠를 제공하는 필터 바텀시트 UI 디자인.

[시각적 디자인 스펙]

1. Scroll Continuation: 화면 10의 `[전체 필터]` 탭을 아래로 스크롤했을 때 이어지는 영역으로 최소 안전 점수·이동 시간 슬라이더 표시
2. Safety Score Filter: 최소 안전 점수를 값 라벨이 있는 수평 슬라이더로 조절
3. Travel Filter: 도보·대중교통 이동 수단과 희망 이동 시간을 토글·슬라이더로 제공
4. Convenience Tab: `[편의시설 필터]` 탭 선택 시 편의점, 카페, 세탁소 등 시설 유형을 아이콘형 선택 버튼으로 표시
5. Selected Facility Filter: 선택한 편의시설 항목에 대해서만 해당 시설의 도보 시간 제한 슬라이더를 노출
6. Bottom Action: `[초기화]`·`[적용완료]` 버튼을 시트 하단에 고정
```

### 📄 화면 12: 지도 매물 선택 상세 바텀시트 UI 디자인

```markdown
[디자인 목표] 지도에서 매물 마커를 선택했을 때, 하단 목록 영역이 선택 매물의 상세 요약으로 전환되는 UI 디자인.

[시각적 디자인 스펙]

1. Selected Map State: 선택한 매물 마커를 강조하고, 매물과 목적지를 잇는 이동·귀갓길 경로를 지도 위 Blue Line으로 표시
2. Detail Bottom Sheet: 기존 매물 목록 바텀시트가 선택 매물의 상세 바텀시트로 전환. Drag Handle과 접기/펼치기 동작 유지
3. Property Summary: 매물 유형 배지, 썸네일, 주소, 보증금·월세, 면적·층수, 관심 하트를 가로형 요약 카드로 배치
4. Navigation: 바텀시트 또는 매물 카드 터치 시 전체 매물 상세 화면으로 연결
```

### 🎨 화면 13: 귀갓길 경로 실시간 평가 모달 UI 디자인

```markdown
[디자인 목표] 선택한 매물의 귀갓길 경로를 실시간으로 평가하고, 사용자의 체감 의견을 받을 수 있는 지도 오버레이 모달 디자인.

[시각적 디자인 스펙]

1. Map Route Overlay: 지도 위 경로를 안전 상태에 따라 Blue 기본선과 주의 구간 강조색으로 표시
2. Evaluation Modal: 상단 또는 중앙에 `귀갓길 경로 실시간 평가` 제목, 짧은 안내 문구, 닫기(X)를 가진 Dark Glass Modal 배치
3. Route Feedback: `이 경로를 이용해 보셨나요?` 질문과 함께 `[완전 럭키비키]`, `[언 럭키비키...]` 등 즉시 평가 버튼 제공
4. Safety Notice: CCTV·가로등·치안시설 등 경로 분석 요소와 주의 구간 안내를 아이콘·상태색으로 간결하게 표시
5. Interaction: 평가 선택 후 토스트 또는 선택 상태를 표시하고, 지도 경로와 선택 매물 바텀시트는 배경에서 유지
```

---

## 📊 PART 4. 매물 상세 및 안전 분석 리포트 (3개 화면)

### 🎨 화면 14: 매물 상세 메인 (매물 요약/안전 점수) UI 디자인

```markdown
[디자인 목표] 화면 12에서 선택한 매물 바텀시트를 위로 스크롤·드래그해 확장했을 때, 매물 상세와 건물 안전 정보를 확인하는 카드형 화면 디자인.

[시각적 디자인 스펙]

1. Expanded Bottom Sheet: 하단에서 위로 확장되는 흰색 바텀시트
2. Sheet Handle: 시트 최상단에는 Drag Handle만 두어 다시 아래로 내리거나 중간 높이로 조절할 수 있게 구성
3. 매물 요약 Panel: 매물 유형 배지, 가로형 썸네일, 주소, 보증금·월세, 면적·층수 등 핵심 정보와 관심 하트 아이콘 배치
4. 건물 안전 정보 Panel: `직접 검증` 상태와 건축물대장 기준 같은 인증 정보, 건물 연식·준공 연도를 좌우 2분할 카드로 구성
5. 안전 점수 Panel: 점수별 색상이 달라지는 원형 Progress Chart, 안전 등급 뱃지, 목적지 기준 안전 분석 문구와 CCTV·가로등·치안시설 지표 표시
6. Detail Link: `[안전 점수 상세보기]` 터치 시 점수 산정 기준 모달 오픈
```

### 🎨 화면 15: 매물 상세 금융 리포트 UI 디자인

```markdown
[디자인 목표] 매물 관련 금융상품과 주변 편의시설을 간결하게 확인하는 Accordion 패널 UI 디자인.

[시각적 디자인 스펙]

1. 금융상품 Accordion: `맞춤 KB 금융 상품` 제목과 Chevron을 가진 접기/펼치기 패널. 펼친 상태에는 상품명, 짧은 설명, 금리·한도 등 핵심 조건과 `[자세히]` 버튼 배치
2. 편의시설 Accordion: `주변 편의시설 (도보 소요 시간)` 제목과 Chevron을 가진 패널. 펼친 상태에는 편의점·카페·다이소 등 시설 아이콘, 거리 또는 도보 시간을 행 단위로 표시
3. 실거주 커뮤니티 Panel: 카테고리 태그와 댓글 수를 헤더에 표시하고, 후기 키워드 태그·안내 문구를 포함한 요약 카드 구성
4. 댓글 진입: 우측의 `[댓글 전체보기]` 링크를 강조하며, 터치 시 전체 댓글 모달로 연결
```

### 🎨 화면 16: 안전 점수 상세 기준 바텀시트 UI 디자인

```markdown
[디자인 목표] 안전 점수 산정 기준과 실거주 댓글을 확인하는 모달 UI 디자인.

[시각적 디자인 스펙]

1. 안전 점수 상세보기 Modal: 상세 화면 위 Dimmed Overlay와 중앙/바텀시트형 흰색 모달. 상단 제목, 닫기(X), 안전 점수·등급 요약 카드 배치
2. 분석 기준 안내: 목적지까지 거리·도보 시간 같은 분석 기준을 상단 요약으로 제시하고, 점수 산정 안내 문구와 정보 아이콘 표시
3. 점수 기준 리스트: 주변 CCTV 분포, 가로등 분포, 치안시설 접근성 등 항목을 컬러 원형 아이콘·항목명·세부 설명의 세로 리스트로 구성
4. 참고 안내: 공공데이터를 바탕으로 한 참고 지표임을 안내하고, 하단에 `[지도에서 안전 요소 보기]` Outlined 버튼 배치
5. 댓글 전체보기 Modal: 댓글 작성자·작성일·본문·공감/신고 액션의 목록과 하단 고정 댓글 입력창, `등록` Primary 버튼 배치
```

---

## 👤 PART 5. 마이페이지 및 내 설정 (4개 화면)

### 🎨 화면 17: 마이페이지 메인 UI 디자인

```markdown
[디자인 목표] 깔끔한 사용자 프로필 및 요약 대시보드 디자인.

[시각적 디자인 스펙]

1. Header: 좌측 뒤로가기 버튼과 중앙 `마이페이지` 타이틀을 가진 흰색 상단 바 구성
2. Profile Card: 아바타, 사용자 정보, `[정보 수정]` Outlined 버튼을 상단 카드로 구성
3. 내 탐색 조건: 목적지, 이동 시간, 보증금·월세를 아이콘+값 리스트로 표시하고 `[수정]` 링크 배치
4. 관심 매물: 안전 점수·가격·하트가 포함된 컴팩트 매물 카드 리스트와 전체보기 동선 배치
5. Account Menu: 비밀번호 변경, 로그아웃, 회원 탈퇴를 아이콘·Chevron 설정 행으로 구분
```

### 🎨 화면 18: 내 주거 조건 수정 UI 디자인

```markdown
[디자인 목표] 온보딩 조건 수정용 Form 재구성 디자인.

[시각적 디자인 스펙]

1. Profile Edit Card: 아바타, 이름·이메일 Input, `[저장하기]` Primary 버튼 구성
2. Password Form: 새 비밀번호·확인 Input, 유효성 안내, 비밀번호 변경 버튼 배치
3. Condition Edit: 목적지, 이동 시간, 예산, 안전 기준을 온보딩과 같은 입력 컴포넌트로 구성
4. Saved Listing: 관심 매물을 썸네일, 가격, 안전 점수, 하트가 있는 세로 리스트로 표시
```

### 🎨 화면 19: 관심 매물 목록 UI 디자인

```markdown
[디자인 목표] 사용자가 저장한 관심 매물을 한눈에 탐색하고 관리하는 목록 UI 디자인.

[시각적 디자인 스펙]

1. Header: 좌측 뒤로가기와 중앙 `관심 매물` 타이틀을 가진 상단 바 구성
2. Listing Card: 매물 썸네일, 유형 배지, 주소, 보증금·월세, 면적, 안전 점수, 관심 하트를 세로 리스트 카드로 배치
3. Safety State: 안전 점수는 상태색 뱃지로 표시하고, 관심 해제는 하트 토글로 제공
4. Interaction: 매물 카드 터치 시 지도 또는 선택 매물 상세 바텀시트로 이동
```

### 🎨 화면 20: 계정 설정 및 탈퇴 모달 UI 디자인

```markdown
[디자인 목표] 계정 설정과 비밀번호 변경·회원 탈퇴 확인 모달을 제공하는 UI 디자인.

[시각적 디자인 스펙]

1. Settings List: 비밀번호 변경, 로그아웃, 회원 탈퇴를 아이콘·Chevron 행으로 구성
2. Password Change Modal: 비밀번호 변경 행 터치 시 Dimmed Overlay 위 모달 오픈. 새 비밀번호·비밀번호 확인 Input, 유효성 안내, `[변경하기]` Primary 버튼과 취소 버튼 배치
3. Danger Zone: `회원 탈퇴` 행을 Red 텍스트·아이콘으로 분리
4. Withdrawal Modal: 탈퇴 행 터치 시 탈퇴 안내와 데이터 삭제 주의 문구, 취소·탈퇴 버튼이 있는 확인 모달 오픈
5. Destructive CTA: 탈퇴는 Red Fill, 취소는 Gray Outline 버튼으로 구분
```
